import { describe, it, expect } from 'vitest';

import { WpmService } from './wpm.service';

describe('WpmService', () => {
  it('should be instantiable', () => {
    expect(WpmService).toBeDefined();
  });
});
