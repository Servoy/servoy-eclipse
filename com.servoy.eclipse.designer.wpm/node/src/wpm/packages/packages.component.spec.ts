import { describe, it, expect } from 'vitest';

import { PackagesComponent } from './packages.component';

describe('PackagesComponent', () => {
  it('should be instantiable', () => {
    expect(PackagesComponent).toBeDefined();
  });
});
